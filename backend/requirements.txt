fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
httpx==0.27.2
pyyaml==6.0.2
pynvml==11.5.3

# Auth (Phase 4)
python-jose[cryptography]==3.3.0
aiosmtplib==3.0.2

# Persistence (Phase 4)
aiosqlite==0.20.0

# RAG + memory (Phase 5) — Qdrant HTTP API is used directly via httpx,
# so we don't need qdrant-client. pypdf handles PDF extraction.
pypdf==5.1.0
python-multipart==0.0.17   # for file upload endpoints

# Per-user encrypted chat-history store (Phase 6):
# AES-256-GCM + HKDF-SHA256 over AUTH_SECRET_KEY (or HISTORY_SECRET_KEY).
cryptography==43.0.3

# Cross-worker rate limiting + scheduler coordination.
# Optional at runtime: the backend falls back to in-memory if REDIS_URL is
# unset or connection fails. In native single-worker mode it goes unused.
redis[hiredis]==5.2.0

# Web search — native mode replaces the SearXNG container with two
# in-process providers. Brave is HTTP-only; ddgs is pure-Python.
ddgs==6.3.0

# Model version resolver — polls Hugging Face for the latest GGUF
# revisions on startup and falls back to pinned versions on failure.
huggingface_hub==0.26.2
